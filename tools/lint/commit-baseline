# Commits at or before this one are not checked.
#
# Four commits reached main with subjects this linter rejects: a scope of
# "tools" that the list did not have, a scope of "capture" where the crate is
# sonduit-capture-win, and two subjects a few characters over the limit.
#
# All four have the same cause. The linter was run after committing rather than
# before, and by then the commit was pushed. History in this repository is the
# only archive there is, so none of them can be rewritten, and recording where
# the rule starts being enforced is the honest alternative to widening the rule
# until the violations fit.
#
# The second pair happened after the first pair had already been recorded here,
# which is what tools/hooks/commit-msg is for. With the hook enabled the
# question cannot arise again, so this line should not need to move.
#
# Move it only for something that genuinely cannot be fixed. Every commit after
# it is checked.
25933916252bd1f64a60cec24f4d39c79c0d6899
