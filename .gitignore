# Rust
/target/
**/target/
*.pdb

# Node
node_modules/
desktop/dist/
desktop/.vite/

# Tauri
desktop/src-tauri/gen/
desktop/src-tauri/.tauri-build/

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk
*.aab
*.keystore
*.jks

# Reference clones for research only. Read, never build, never vendor.
# scream-android is GPL-3.0 and AndroidUsbAudioDevice has no licence at all;
# neither may ever enter this repository. See docs/licensing.md.
third_party/reference/

# Signing keys and secrets
*.key
*.key.pub
.env

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp

# FFmpeg is fetched by tools/fetch-ffmpeg.mjs, never committed.
desktop/src-tauri/binaries/

# Android build output, and the local SDK path. local.properties names a
# machine-specific directory and must never be committed.
android/.gradle/
android/build/
android/app/build/
android/local.properties
